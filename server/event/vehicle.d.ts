// Autogenerated file.
// DO NOT EDIT. ANY CHANGES WILL BE OVERWRITTEN

import { EventNames } from './all_event_names';
import {
    TextDisplay,
    Account,
    ACL,
    ACLGroup,
    Ban,
    XML,
    Userdata,
    TextItem,
    Pickup,
    Request,
    Player,
    Blip,
    ColShape,
    Element,
    Ped,
    Resource,
    Team,
    Vehicle,
    XmlNode,
    File,
    Marker,
    MTASAObject,
    RadarArea,
    Water,
    Timer,
    HandleFunction,
    TimerCallbackFunction,
    FetchRemoteCallback,
    GenericEventHandler,
    CommandHandler,
    BindKeyCallback,
    BindKeyCallbackVarArgs,
    ControlName,
    KeyName,
    KeyState,
    Vector2,
    Vector3,
    Vector4,
    Matrix,
} from '../structure';

export interface OnTrailerAttach extends GenericEventHandler {
    name: EventNames.OnTrailerAttach;
    function: (this: void, theTruck: Vehicle) => void;
}

export interface OnTrailerDetach extends GenericEventHandler {
    name: EventNames.OnTrailerDetach;
    function: (this: void, theTruck: Vehicle) => void;
}

export interface OnVehicleDamage extends GenericEventHandler {
    name: EventNames.OnVehicleDamage;
    function: (this: void, loss: number) => void;
}

export interface OnVehicleEnter extends GenericEventHandler {
    name: EventNames.OnVehicleEnter;
    function: (this: void, thePed: Ped, seat: number, jacked: Player) => void;
}

export interface OnVehicleExit extends GenericEventHandler {
    name: EventNames.OnVehicleExit;
    function: (
        this: void,
        thePed: Ped,
        seat: number,
        jacker: Ped,
        forcedByScript: boolean,
    ) => void;
}

export interface OnVehicleExplode extends GenericEventHandler {
    name: EventNames.OnVehicleExplode;
    function: (this: void) => void;
}

export interface OnVehicleRespawn extends GenericEventHandler {
    name: EventNames.OnVehicleRespawn;
    function: (this: void, exploded: boolean) => void;
}

export interface OnVehicleStartEnter extends GenericEventHandler {
    name: EventNames.OnVehicleStartEnter;
    function: (this: void, enteringPed: Ped, seat: number, jacked: Ped) => void;
}

export interface OnVehicleStartExit extends GenericEventHandler {
    name: EventNames.OnVehicleStartExit;
    function: (
        this: void,
        exitingPed: Ped,
        seat: number,
        jacked: Ped,
        door: number,
    ) => void;
}
