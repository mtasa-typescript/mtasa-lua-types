import { mtasa } from 'client';

const colCircle: mtasa.ColShape = mtasa.createColCircle(1, 1, 1);
const colCuboid: mtasa.ColShape = mtasa.createColCuboid(13, 14, 15, 16, 17, 18);
const colRectangle: mtasa.ColShape = mtasa.createColRectangle(0, 0, 1, 2);
const colSphere: mtasa.ColShape = mtasa.createColSphere(13, 14, 15, 200);
const colTube: mtasa.ColShape = mtasa.createColTube(4, 5, 6, 100, 100);
